<UserControl x:Class="WindowModules.Wpf.WindowActionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DesignWidth="500"
             mc:Ignorable="d"
             >
    <Grid>
        <Grid.Resources>
        <Style x:Key="SeparatorStyle" TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" Value="1,0,0,0"/>
            <Setter Property="BorderBrush" Value="#A5A5A5A5"/>
            <Setter Property="Margin" Value="10,0,10,0"/>
            <Setter Property="Width" Value="2"/>
            <Setter Property="Background" Value="LightGray"/>
        </Style>
        </Grid.Resources>
        <TextBlock Name="textIndex" Text="10" VerticalAlignment="Center" Width="20" HorizontalAlignment="Left" Margin="5,0,0,0" />
        <Border HorizontalAlignment="Left" Margin="30,5,0,5" Style="{StaticResource SeparatorStyle}" />
        <ContentControl Name="control1" Margin="40,0,0,0" Width="400" HorizontalAlignment="Left" />
        <Button Content="Delete" HorizontalAlignment="Right" Width="50" Height="30" Margin="0,0,5,0" Click="ButtonDelete_Click" />
    </Grid>
</UserControl>
